# Auth + Progress Sync — Implementation Plan (✅ completed & shipped)

Implemented and live: optional Clerk sign-in + cross-device progress sync to Railway Postgres
(server-side merge, newest-wins). Full task list preserved in git history.

For how it works now, see the *Auth & progress sync* section of **[AGENTS.md](../../../AGENTS.md)**.
The remaining step (promote Clerk dev→production) is tracked in `docs/CHECKLIST-production.md`.
